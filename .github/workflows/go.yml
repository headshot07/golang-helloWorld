name: helloWorld
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    container:
    steps:

      - uses: actions/checkout@v3
#      - name: Debug
#        run: |
#          pwd
#          echo ${HOME}
#          echo ${GITHUB_WORKSPACE}
#          echo ${GOPATH}
#          echo ${GOROOT}

      - name: Set Up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.18

#      - name: Build
#        run: go build -v ./...
#
#      - name: Test
#        run: go test -v ./...
