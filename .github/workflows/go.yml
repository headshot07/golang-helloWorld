name: helloWorld
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:latest

      golang:
        image: golang:latest
#    container:
#      image: golang:1.18
    steps:

      - uses: actions/checkout@v3

#      - name: Build
#        run: go build -v ./...

      - name: Test
        run: go test -v ./...

      - name: Setup Database
        run: psql -h postgres -U postgres -c 'CREATE DATABASE "postgres_db";'